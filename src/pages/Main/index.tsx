import { BaseTemplate } from '../../templates';
import { MainContainerItem } from '../../components/index';

export const Main = () => {
  return (
    <BaseTemplate>
      <MainContainerItem />
    </BaseTemplate>
  );
};
