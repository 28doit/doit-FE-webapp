import { BaseTemplate } from '../../templates';
import { MainContainer } from '../../components/index';

export const Main = () => {
  return (
    <BaseTemplate>
      <MainContainer />
    </BaseTemplate>
  );
};
