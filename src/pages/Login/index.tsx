import { BaseTemplate } from '../../templates';
import { LoginModal } from '../../components/index';

export const NewLoginForm = () => {
  return (
    <BaseTemplate>
      <LoginModal />
    </BaseTemplate>
  );
};
