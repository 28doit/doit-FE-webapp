import { BaseTemplate } from '../../templates';
import { ImageDetail } from '../../components/index';

export const ImgDetail = () => {
  return (
    <BaseTemplate>
      <ImageDetail />
    </BaseTemplate>
  );
};
