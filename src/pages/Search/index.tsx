import { BaseTemplate } from '../../templates';
import { SearchContainer } from '../../components/index';

export const Search = () => {
  return (
    <BaseTemplate>
      <SearchContainer />
    </BaseTemplate>
  );
};
